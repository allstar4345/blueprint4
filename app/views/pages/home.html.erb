<% if signed_in? %>

  <table class="front" summary="For signed-in users">
    <tr>
      <td class="main">
        <h1 class="micropost">Welcome to Family Connect</h1>
        <%= render 'shared/micropost_form' %>
        <%= render 'shared/feed' %>
      </td>
      <td class="sidebar round">
        <%= render 'shared/user_info' %>
        <%= render 'shared/stats' %>
      </td>
    </tr>
  </table>

<% else %>

<h1>The Blueprint Sports</h1>
<p>he Blueprint Sports is analysis, analytics, statistics, and opinions at the highest level about everything sports.   

<%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>